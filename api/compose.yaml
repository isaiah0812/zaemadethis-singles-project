services:
  api:
    container_name: singles-api
    build:
      context: .
      dockerfile: ./dev.Dockerfile
    ports:
      - 8080:8080
    develop:
      watch:
        - action: sync+restart
          path: ./
          target: ./api
